.vercel

# Environment & Secrets (AIOX)
.env
.env.local
.env.*.local
.env.backup*
*.key
*.pem

# Dependencies (AIOX)
node_modules/
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# AIOX Framework (local-only, instalado via @aiox-squads/core; não vai para repo público)
.aiox-core/
.aiox/
squads/
pro-installed-manifest.yaml
pro-version.json
AGENTS.md

# IDE adapter folders (local-only)
.antigravity/
.codex/
.cursor/
.gemini/
.kimi/

# Claude Code config (settings.json pode ter rules privadas)
.claude/

# CI/CD adapter (workflows não publicar)
.github/

# Residual/temp files
patch*.py
copy-showcase-site.md
